# Healthy baseline scenario overlay
# No breakage - this is the working reference deployment
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: demo-healthy

resources:
  - ../../../base
  - httproute.yaml

patches:
  - path: patch-configmap.yaml
    target:
      kind: ConfigMap
      name: fider-env
