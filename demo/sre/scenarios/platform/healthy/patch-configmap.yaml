# Patch to set correct BASE_URL for healthy scenario
# No breakage introduced - this is the working baseline
apiVersion: v1
kind: ConfigMap
metadata:
  name: fider-env
data:
  BASE_URL: "http://healthy.localhost:8080"
